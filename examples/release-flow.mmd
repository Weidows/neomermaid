%% Release pipeline — Subgraphs, decision diamonds, nested shapes and per-class colours.
%% preset hint: subgraphs, classDef, curved edges
flowchart LR
  subgraph dev[" Local "]
    code["Write code"] --> test{{"Tests"}}
    test -- "pass" --> lint["Lint"]
    test -- "fail" --> code
  end

  subgraph ci[" CI "]
    lint --> build["Build artifact"]
    build --> sign{"Signed?"}
    sign -- "no" --> build
    sign -- "yes" --> publish
  end

  subgraph prod[" Production "]
    publish["Publish release"] --> canary(["Canary 5%"])
    canary --> full(["Full rollout"])
  end

  classDef danger stroke:#f85149,stroke-width:3px;
  classDef ok stroke:#3fb950,stroke-width:3px;
  class sign danger;
  class full ok;
